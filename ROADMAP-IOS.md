# Snapcost → iOS: покроковий план

Оновлено: 2026-07-05. Ролі: **[ТИ]** — дії руками (5–15 хв кожна), **[КЛОД]** — пишу код я.

---

## Етап 1. Деплой на Vercel (безкоштовно) — БЛОКУЄ ВСЕ ІНШЕ

Без цього не працюють пуші з закритим застосунком і нема URL для iOS-оболонки.

1. **[ТИ]** Зайди на vercel.com → Sign up через GitHub → "Add New Project" → вибери репозиторій `marathon-budget-tracker` → Import.
2. **[ТИ]** Перед Deploy відкрий "Environment Variables" і додай усі ключі зі свого файлу `.env.local` (скопіюй name/value один в один):
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `ANTHROPIC_API_KEY`
   - `NEXT_PUBLIC_VAPID_PUBLIC_KEY`
   - `VAPID_PRIVATE_KEY`
   - `VAPID_SUBJECT`
   - `CRON_SECRET`
3. **[ТИ]** Натисни Deploy. Отримаєш адресу типу `https://snapcost-xxxx.vercel.app` — скинь її мені.
4. **[ТИ]** У Supabase: Authentication → URL Configuration → додай цю адресу в Site URL і Redirect URLs (щоб працював вхід через Google).
5. **[ТИ]** Крон (щоб нагадування приходили щогодини): зареєструйся на cron-job.org (безкоштовно) → Create cronjob:
   - URL: `https://ТВОЯ-АДРЕСА.vercel.app/api/cron/notify`
   - Schedule: every hour (хвилина 0)
   - Headers: `Authorization` = `Bearer ТВІЙ_CRON_SECRET` (значення з .env.local)
6. **[ТИ]** Тест на iPhone: Safari → відкрий адресу → Поділитися → «На екран Домівки» → відкрий з іконки → Нагадування → «Дозволити сповіщення». Створи нагадування на найближчу годину, закрий застосунок — пуш має прийти.

## Етап 2. Обкатка вебу (1–2 тижні користування)

- **[ТИ]** Користуйся щодня з телефона: сканування чеків, валюти, звіти. Все, що дратує/глючить — кажи мені.
- **[КЛОД]** Фікшу все знайдене.

## Етап 3. Capacitor (iOS-оболонка) — вже підготовлено

Код уже в репозиторії: `capacitor.config.json` + залежності в package.json.

1. **[КЛОД]** Впишу твою Vercel-адресу в `capacitor.config.json` (зараз там заглушка).
2. **[ТИ]** Встанови Xcode з Mac App Store (безкоштовно, ~10 ГБ) і виконай в терміналі в папці проєкту:
   ```
   npm install
   npx cap add ios
   npm run ios:open
   ```
   Відкриється Xcode з готовим проєктом. Запусти на симуляторі кнопкою ▶.
3. **[КЛОД]** Далі додам нативні пуші (APNs), камеру, іконки, splash — щоб пройти правило Apple 4.2.

## Етап 4. Публікація (тут перші витрати)

1. **[ТИ]** Apple Developer акаунт — $99/рік (developer.apple.com).
2. **[ТИ]** Матеріали: іконка 1024×1024 (без прозорості), скріншоти 6–10 шт., тексти (назва до 30 символів, підзаголовок до 30, опис, ключові слова), Support URL.
3. **[КЛОД]** Допоможу зібрати архів у Xcode → TestFlight (бета на твій телефон) → подача на рев'ю.
4. Рев'ю Apple: 2–7 днів. Якщо будуть зауваження — правимо і подаємо знову.

---

**Поточний статус:** Етап 1 не почато (це твій наступний крок). Privacy Policy (/privacy) ✓, видалення акаунта ✓, i18n ✓, Capacitor-конфіг ✓.
