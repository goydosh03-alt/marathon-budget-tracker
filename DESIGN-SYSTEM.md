# Snapcost — Design System (v1)

Жива "конституція" дизайну. Перш ніж вставляти будь-який компонент — він має бути тут. Новий компонент → спершу узгодити → додати сюди → аж тоді вживати.

---

## 1. Принципи

- **Mobile-first PWA**, темна тема.
- **Чистий стиль** — без `backdrop-filter`/скла. Глибина через градієнти, м'які тіні, прозорість.
- **4-кратна сітка:** усі відступи / padding / margin / gap кратні 4 (4, 8, 12, 16, 20, 24, 32). Auto-layout.
- **Локалізація:** інтерфейс українською.

---

## 2. Кольори

| Роль | Hex |
|---|---|
| Фон (база) | `#05090b` |
| Фон-градієнт верх | `#0c1a1d` → `#091316` → `#05090b` |
| Glow-засвітки фону | teal `rgba(74,222,180,.20)`, blue `rgba(78,150,255,.16)`, violet `rgba(120,90,255,.10)` |
| Акцент 1 (зелений) | `#4ade9f` / світліший `#6ee7b7` |
| Акцент 2 (синій) | `#3bb4f5` / `#7cc8f5` |
| Градієнт-акцент | `linear-gradient(135deg,#4ade9f,#3bb4f5)` |
| Категорія "фіолет" | `#b9a8ff` на `rgba(124,92,255,.16)` |
| Текст основний | `#eaf2f0` |
| Текст вторинний | `rgba(255,255,255,.5)` |
| Поверхня картки | `rgba(255,255,255,.04)` + бордер `rgba(255,255,255,.06)` |
| Поверхня панелі (навбар) | `rgba(18,28,30,.97)` + бордер `rgba(255,255,255,.07)` |

Кожна сума у двох валютах: base (`$`) великим + home (`≈ zł`) приглушеним.

---

## 3. Типографіка

- Шрифт: **Inter** (в застосунку — `next/font`).
- Велике число (баланс): 40px / 700, `letter-spacing:-1.5px`.
- Сума в блоці: 26px / 700.
- Заголовок секції: 15px / 600.
- Тіло: 14px / 500–600.
- Підписи: 11–12px, вторинний колір.

## 4. Радіуси

- Картки: 16–20px. Великі панелі/навбар: 26–28px. Тайли іконок: 9–12px. Пігулки/чипи: 999px.

---

## 5. Іконки

- **Категорії** (їжа, кафе, транспорт, зарплата...) → **емоджі** (барвисто): 🛒 ☕ 🚌 💰. Тимчасово; легко поміняти на залиті іконки.
- **Системні / структурні** (баланс, рахунки, дзвоник, валюта, меню, фільтри) → **залиті (filled) Material Symbols** (Google, Apache 2.0). У концепті — inline SVG, у застосунку — Material Symbols Rounded.
- Привітання "Привіт 👋" — емоджі-рука.

---

## 6. Верхньорівневі елементи головної (зверху вниз)

1. **Шапка:** аватар + `Привіт / Ім'я` · чип валюти `$ USD ⌄` · кнопка сповіщень 🔔.
2. **Загальний баланс** = сума всіх рахунків. Велике число + `≈ zł`.
3. **Рахунки:** горизонтальні картки `Готівка` / `Картка` / `➕` (додати рахунок). Іконки 👛 💳 (Material).
4. **Великі таби:** `Витрати` | `Дохід` (segmented, активний — градієнт-акцент). Верхньорівневий перемикач режиму.
5. **Блок-період** (одне ціле):
   - фільтр `День · Тиждень · Місяць · Рік` **|** `📅 Період` (вертикальна паличка 14px перед календарем; календар відкриває модалку вибору дат);
   - сума за період + бюджет (горизонтально) + прогрес-бар;
   - **повна лінія**;
   - **список транзакцій**, що оновлюється під вибраний період і таб.
6. **Навбар (док):** `Головна · Історія · Звіти · Профіль` (eBay-style, на всю ширину, активний підсвічений) + **окрема нейтральна кнопка `+`** збоку.

---

## 7. Компоненти

- **Картка-рахунок:** поверхня картки, тайл-іконка (Material) + назва, сума великим + `≈ zł`.
- **Транзакція (ReceiptCard):** емоджі-тайл категорії · назва + `категорія · дата` · сума (`+` дохід зеленим `#6ee7b7`, `−` витрата білим) + `zł` приглушено.
- **Сегмент-контрол:** таби (Витрати/Дохід) і фільтр періоду — однаковий патерн пігулки з активним станом.
- **Навбар + кнопка `+`:** обидва — нейтральна поверхня панелі, без унікального акценту. Активний таб навбару підсвічений `rgba(255,255,255,.11)`.

---

## 8. Поведінка (логіка взаємодій)

- **Кнопка `+`** → стає активною (підсвічена) і відкриває **дропдаун-меню** над собою:
  - `⬆ Додати дохід` — **дивайдер на всю ширину** — `✎ Витрата вручну` · `🔲 Сканувати чек` (камера).
- **Таби Витрати/Дохід** → перемикають режим: сума й список показують відповідно витрати або доходи.
- **Фільтр періоду** (День/Тиждень/Місяць/Рік) → оновлює суму + список під обраний період.
- **Період (📅)** → відкриває модалку календаря (вибір довільних дат).
- **Чип валюти** → перемикання/вибір валюти обліку (USD/PLN/HUF...).
- **Форма додавання** (bottom sheet) — відкривається з меню "+": сегмент Витрата/Дохід, сума (zł), категорія, назва, рахунок, дата → Зберегти (server action → база → refresh).

> Стан v1: усі іконки **залиті (filled)**, нав теж. Таби активні — **зелений градієнт** (без жовтого). Відступи стиснуті (4-сітка). Курс валют тимчасово фіксований (≈4 zł/$) до підключення ExchangeRate API.

---

## 9. Що НЕ робимо (v1)

Без скла/блюру, без банк-синхронізації (Plaid), без акцій/крипти. Скоуп — [[snapcost-project]]: рахунки + витрати + доходи + баланс + бюджет + конвертація валют.
